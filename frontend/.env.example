# FILE: frontend/.env.example
# Copy to .env.local for local development

# Backend API base URL
VITE_API_URL=http://localhost:5000/api/v1

# For production (Vercel / Docker nginx proxy):
# VITE_API_URL=/api/v1
